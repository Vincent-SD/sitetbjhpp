{% extends 'base.html.twig' %}

{% block title %}Phare{% endblock %}

{% block body %}
    <h1>Phare</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phare.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phare.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ phare.description }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ phare.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ phare.latitude }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ phare.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('phare_index') }}">back to list</a>

    <a href="{{ path('phare_edit', {'id': phare.id}) }}">edit</a>

    {{ include('phare/_delete_form.html.twig') }}
{% endblock %}
